import { LoginCard } from "../../Components/Auth/LoginCard";

export default function Login() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-primaryDark via-neutralDark to-black text-gray-900 dark:text-gray-100">
      <LoginCard />
    </div>
  );
}
